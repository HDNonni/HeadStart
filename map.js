window.onload = init;
function init(){
    let map = document.getElementById("map");

}

function showCoords(eventObj){
    let map = document.getElementById("map")


    map.innerHTML = "Map Coordinates: " + x + ", " + y;
}